fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pillow>=10.0
numpy>=1.26

# --- Wave 2 (Agent C) adds real inference dependencies here: ---
# torch (CPU), sam2 or ultralytics SAM port, opencv-python-headless

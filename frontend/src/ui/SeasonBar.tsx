/**
 * OWNED BY AGENT D (Wave 3). Placeholder — becomes the season phase toggle
 * bar (display mode / early-spring ... winter) and timelapse playback.
 */
export function SeasonBar() {
  return null;
}

/**
 * OWNED BY AGENT E (Wave 3). Placeholder — becomes photo upload, reference
 * layer controls, calibration flow, and click-to-segment UX (talks to the
 * backend through ./api.ts).
 */
export function PhotoPanel() {
  return null;
}
